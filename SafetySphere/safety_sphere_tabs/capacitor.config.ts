import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'safety_sphere_tabs',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
